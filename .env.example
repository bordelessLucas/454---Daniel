# URL base da API (sem barra no final)
VITE_API_URL=http://localhost:3000

# Geração de PDF: server (Puppeteer no backend) | client (react-pdf no browser, legado)
VITE_PDF_GENERATOR=server

# URL absoluta do logo no PDF do relatório (opcional)
# VITE_RELATORIO_LOGO_URL=https://exemplo.com/logo.png
